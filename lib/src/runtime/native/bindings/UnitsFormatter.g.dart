// generated by diplomat-tool
// dart format off

part of 'lib.g.dart';

/// An ICU4X unit formatter pinned to a single CLDR unit identifier
/// (e.g. "meter", "kilometer-per-hour", "hour"). Renders values like
/// "5 hours", "5 hr", "5 h" depending on width.
///
/// See the [Rust documentation for `UnitsFormatter`](https://docs.rs/icu/2.2.0/icu/experimental/dimension/units/formatter/struct.UnitsFormatter.html) for more information.
final class UnitsFormatter implements ffi.Finalizable {
  final ffi.Pointer<ffi.Opaque> _ffi;

  // These are "used" in the sense that they keep dependencies alive
  // ignore: unused_field
  final core.List<Object> _selfEdge;

  // This takes in a list of lifetime edges (including for &self borrows)
  // corresponding to data this may borrow from. These should be flat arrays containing
  // references to objects, and this object will hold on to them to keep them alive and
  // maintain borrow validity.
  UnitsFormatter._fromFfi(this._ffi, this._selfEdge) {
    if (_selfEdge.isEmpty) {
      _finalizer.attach(this, _ffi.cast());
    }
  }

  @_DiplomatFfiUse('icu4x_UnitsFormatter_destroy_mv1')
  static final _finalizer = ffi.NativeFinalizer(ffi.Native.addressOf(_icu4x_UnitsFormatter_destroy_mv1));

  /// Creates a new [UnitsFormatter] for `unit_identifier` using compiled
  /// CLDR data.
  ///
  /// `unit_identifier` is a CLDR unit identifier (e.g. "meter",
  /// "kilometer", "kilometer-per-hour", "hour"). Returns an error if the
  /// locale or unit has no data.
  ///
  /// See the [Rust documentation for `try_new`](https://docs.rs/icu/2.2.0/icu/experimental/dimension/units/formatter/struct.UnitsFormatter.html#method.try_new) for more information.
  ///
  /// Throws [DataError] on failure.
  factory UnitsFormatter.forUnit(Locale locale, String unitIdentifier, [UnitsWidth? width]) {
    final temp = _FinalizedArena();
    final result = _icu4x_UnitsFormatter_create_for_unit_mv1(locale._ffi, unitIdentifier._utf8AllocIn(temp.arena), width != null ? _ResultInt32Void.ok(width.index) : _ResultInt32Void.err());
    if (!result.isOk) {
      throw DataError.values[result.union.err];
    }
    return UnitsFormatter._fromFfi(result.union.ok, []);
  }

  ///
  ///
  /// Throws [DataError] on failure.
  factory UnitsFormatter.forUnitWithProvider(DataProvider provider, Locale locale, String unitIdentifier, [UnitsWidth? width]) {
    final temp = _FinalizedArena();
    final result = _icu4x_UnitsFormatter_create_for_unit_with_provider_mv1(provider._ffi, locale._ffi, unitIdentifier._utf8AllocIn(temp.arena), width != null ? _ResultInt32Void.ok(width.index) : _ResultInt32Void.err());
    if (!result.isOk) {
      throw DataError.values[result.union.err];
    }
    return UnitsFormatter._fromFfi(result.union.ok, []);
  }

  /// Format `value` with the unit. Returns the locale-correct plural
  /// form (e.g. "1 hour" / "2 hours" in en-US).
  ///
  /// See the [Rust documentation for `format_fixed_decimal`](https://docs.rs/icu/2.2.0/icu/experimental/dimension/units/formatter/struct.UnitsFormatter.html#method.format_fixed_decimal) for more information.
  String format(Decimal value) {
    final write = _Write();
    _icu4x_UnitsFormatter_format_mv1(_ffi, value._ffi, write._ffi);
    return write.finalize();
  }

  /// Format `value` into typed parts (ECMA-402 `formatToParts` shape):
  /// integer / group / decimal / fraction, with the unit name as a
  /// single `unit` part and surrounding spacing as `literal`.
  FormattedNumberParts formatToParts(Decimal value) {
    final result = _icu4x_UnitsFormatter_format_to_parts_mv1(_ffi, value._ffi);
    return FormattedNumberParts._fromFfi(result, []);
  }

}

@_DiplomatFfiUse('icu4x_UnitsFormatter_destroy_mv1')
@ffi.Native<ffi.Void Function(ffi.Pointer<ffi.Void>)>(isLeaf: true, symbol: 'icu4x_UnitsFormatter_destroy_mv1')
// ignore: non_constant_identifier_names
external void _icu4x_UnitsFormatter_destroy_mv1(ffi.Pointer<ffi.Void> self);

@_DiplomatFfiUse('icu4x_UnitsFormatter_create_for_unit_mv1')
@ffi.Native<_ResultOpaqueInt32 Function(ffi.Pointer<ffi.Opaque>, _SliceUtf8, _ResultInt32Void)>(isLeaf: true, symbol: 'icu4x_UnitsFormatter_create_for_unit_mv1')
// ignore: non_constant_identifier_names
external _ResultOpaqueInt32 _icu4x_UnitsFormatter_create_for_unit_mv1(ffi.Pointer<ffi.Opaque> locale, _SliceUtf8 unitIdentifier, _ResultInt32Void width);

@_DiplomatFfiUse('icu4x_UnitsFormatter_create_for_unit_with_provider_mv1')
@ffi.Native<_ResultOpaqueInt32 Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>, _SliceUtf8, _ResultInt32Void)>(isLeaf: true, symbol: 'icu4x_UnitsFormatter_create_for_unit_with_provider_mv1')
// ignore: non_constant_identifier_names
external _ResultOpaqueInt32 _icu4x_UnitsFormatter_create_for_unit_with_provider_mv1(ffi.Pointer<ffi.Opaque> provider, ffi.Pointer<ffi.Opaque> locale, _SliceUtf8 unitIdentifier, _ResultInt32Void width);

@_DiplomatFfiUse('icu4x_UnitsFormatter_format_mv1')
@ffi.Native<ffi.Void Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)>(isLeaf: true, symbol: 'icu4x_UnitsFormatter_format_mv1')
// ignore: non_constant_identifier_names
external void _icu4x_UnitsFormatter_format_mv1(ffi.Pointer<ffi.Opaque> self, ffi.Pointer<ffi.Opaque> value, ffi.Pointer<ffi.Opaque> write);

@_DiplomatFfiUse('icu4x_UnitsFormatter_format_to_parts_mv1')
@ffi.Native<ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)>(isLeaf: true, symbol: 'icu4x_UnitsFormatter_format_to_parts_mv1')
// ignore: non_constant_identifier_names
external ffi.Pointer<ffi.Opaque> _icu4x_UnitsFormatter_format_to_parts_mv1(ffi.Pointer<ffi.Opaque> self, ffi.Pointer<ffi.Opaque> value);

// dart format on
