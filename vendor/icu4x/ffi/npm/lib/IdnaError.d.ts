// generated by diplomat-tool
import type { pointer, codepoint } from "./diplomat-runtime.d.ts";



/**
 * Errors that can occur during IDNA processing.
 *
 * `Invalid` covers all UTS #46 / RFC 5891 validation failures —
 * the `idna` crate aggregates many specific errors into a single
 * `Errors` opaque type, so we collapse them here for the FFI surface.
 */
export class IdnaError {

    /** @internal */
    static fromValue(value: IdnaError | string): IdnaError;

    get value(): string;

    /** @internal */
    get ffiValue(): number;

    static Unknown : IdnaError;
    static InvalidUtf8 : IdnaError;
    static Invalid : IdnaError;


    constructor(value: IdnaError | string );
}