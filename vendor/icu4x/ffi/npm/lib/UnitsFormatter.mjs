// generated by diplomat-tool
import { DataError } from "./DataError.mjs"
import { DataProvider } from "./DataProvider.mjs"
import { Decimal } from "./Decimal.mjs"
import { DecimalGroupingStrategy } from "./DecimalGroupingStrategy.mjs"
import { FormattedNumberParts } from "./FormattedNumberParts.mjs"
import { Locale } from "./Locale.mjs"
import { UnitsWidth } from "./UnitsWidth.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const UnitsFormatter_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.icu4x_UnitsFormatter_destroy_mv1(ptr);
});

/**
 * An ICU4X unit formatter pinned to a single CLDR unit identifier
 * (e.g. "meter", "kilometer-per-hour", "hour"). Renders values like
 * "5 hours", "5 hr", "5 h" depending on width.
 *
 * See the [Rust documentation for `UnitsFormatter`](https://docs.rs/icu/2.2.0/icu/experimental/dimension/units/formatter/struct.UnitsFormatter.html) for more information.
 */
export class UnitsFormatter {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("UnitsFormatter is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            UnitsFormatter_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Creates a new {@link UnitsFormatter} for `unit_identifier` using compiled
     * CLDR data.
     *
     * `unit_identifier` is a CLDR unit identifier (e.g. "meter",
     * "kilometer", "kilometer-per-hour", "hour"). Returns an error if the
     * locale or unit has no data.
     *
     * See the [Rust documentation for `try_new`](https://docs.rs/icu/2.2.0/icu/experimental/dimension/units/formatter/struct.UnitsFormatter.html#method.try_new) for more information.
     */
    static createForUnit(locale, unitIdentifier, width, groupingStrategy) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const unitIdentifierSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, unitIdentifier)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.icu4x_UnitsFormatter_create_for_unit_mv1(diplomatReceive.buffer, locale.ffiValue, unitIdentifierSlice.ptr, diplomatRuntime.optionToBufferForCalling(wasm, width, 4, 4, functionCleanupArena, (arrayBuffer, offset, jsValue) => [diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, jsValue.ffiValue, Int32Array)]), diplomatRuntime.optionToBufferForCalling(wasm, groupingStrategy, 4, 4, functionCleanupArena, (arrayBuffer, offset, jsValue) => [diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, jsValue.ffiValue, Int32Array)]));

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new DataError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('DataError.' + cause.value, { cause });
            }
            return new UnitsFormatter(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
        }
    }

    static createForUnitWithProvider(provider, locale, unitIdentifier, width, groupingStrategy) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const unitIdentifierSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, unitIdentifier)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.icu4x_UnitsFormatter_create_for_unit_with_provider_mv1(diplomatReceive.buffer, provider.ffiValue, locale.ffiValue, unitIdentifierSlice.ptr, diplomatRuntime.optionToBufferForCalling(wasm, width, 4, 4, functionCleanupArena, (arrayBuffer, offset, jsValue) => [diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, jsValue.ffiValue, Int32Array)]), diplomatRuntime.optionToBufferForCalling(wasm, groupingStrategy, 4, 4, functionCleanupArena, (arrayBuffer, offset, jsValue) => [diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, jsValue.ffiValue, Int32Array)]));

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new DataError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('DataError.' + cause.value, { cause });
            }
            return new UnitsFormatter(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
        }
    }

    /**
     * Format `value` with the unit. Returns the locale-correct plural
     * form (e.g. "1 hour" / "2 hours" in en-US).
     *
     * See the [Rust documentation for `format_fixed_decimal`](https://docs.rs/icu/2.2.0/icu/experimental/dimension/units/formatter/struct.UnitsFormatter.html#method.format_fixed_decimal) for more information.
     */
    format(value) {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.icu4x_UnitsFormatter_format_mv1(this.ffiValue, value.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    /**
     * Format `value` into typed parts (ECMA-402 `formatToParts` shape):
     * integer / group / decimal / fraction, with the unit name as a
     * single `unit` part and surrounding spacing as `literal`.
     */
    formatToParts(value) {

        const result = wasm.icu4x_UnitsFormatter_format_to_parts_mv1(this.ffiValue, value.ffiValue);

        try {
            return new FormattedNumberParts(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}